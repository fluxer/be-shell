#!/bin/sh
export DISPLAY=:0 # qdbus needs a the X server
UPDATES=$(aptitude search "~U") # query updates

if [ -z "$UPDATES" ]; then
    COUNT=0
else
    COUNT=`echo "$UPDATES" | wc -l`
fi

qdbus org.kde.be.shell /Apt setText "$COUNT"
qdbus org.kde.be.shell /Apt setToolTip "$UPDATES"
